name = "in-your-base-assistant"
main = "src/index.js"
compatibility_date = "2025-01-01"

# --- Non-secret config ---
# Exact origin the hosted/index.html clone is served from (no trailing
# slash). Requests from any other Origin are rejected before the passcode
# is even checked. Example: "https://yourname.github.io"
[vars]
ALLOWED_ORIGIN = "https://laustenfaund.github.io"

# Cents, not dollars. $10.00/day per passcode, $40.00/month total.
# Sized for multi-turn tool-calling (search/read the archive) — one user
# question can fire several upstream requests, not just one. Adjust to
# taste once you've seen real usage.
PER_USER_DAILY_CAP_CENTS = "1000"
GLOBAL_MONTHLY_CAP_CENTS = "4000"

# --- KV namespaces ---
# Create with `wrangler kv namespace create USAGE` and
# `wrangler kv namespace create PASSCODES`, then paste the printed ids below.
[[kv_namespaces]]
binding = "USAGE"
id = "REPLACE-WITH-USAGE-NAMESPACE-ID"

[[kv_namespaces]]
binding = "PASSCODES"
id = "REPLACE-WITH-PASSCODES-NAMESPACE-ID"

# --- Secret ---
# Set with: wrangler secret put ANTHROPIC_API_KEY
# Never put the real key in this file.
